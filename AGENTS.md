<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# DC Organizer — Agent, Design System & Engineering Rules

These rules apply to the entire repository unless a more specific implementation requirement says otherwise.

## 1. Required Workflow Before Coding

Before any material change, inspect in this order:

1. `AGENTS.md`
2. `prd.md`
3. `README.md`
4. Relevant implementation files, routes, components, schema, and APIs

`prd.md` is the single source of truth for active product requirements **and** the consolidated implementation history in Appendix A.

Every AI-made material change MUST update Appendix A in `prd.md` with rationale, affected files/area, commit(s), and validation status. If a change alters product requirements, update the canonical body section first and then append its implementation/history entry in the same `prd.md`.

Do not create or revive `prd1.md`, `prdnew.md`, `PRD2.md`, `PRD3.md`, or other split PRD files. When the owner **explicitly requests `prd-tambahan.md`**, it may exist only as a supplemental non-canonical delta log; active requirements must still be mirrored to `prd.md`, and `prd-tambahan.md` must never override the canonical body or Appendix A.

Do not add generic project-wide prompt/skill documents that duplicate or conflict with `AGENTS.md` and `prd.md`. If specialized instructions are genuinely needed, keep them narrowly scoped and explicitly referenced.

Validation must never be described as PASS unless an actual build/CI result is available.

## 2. Product Identity & Preservation

- Official product brand: **DC Organizer**.
- **Protected brand wordmark contract:** customer-facing `DC Organizer` wordmarks MUST render through `components/Brand/BrandWordmark.tsx` (or preserve its exact output when technically impossible to import it). The wordmark font is `var(--font-dc-heading)` / Cinzel, its wording and Rose treatment are locked, and agents must not reinterpret, restyle, substitute, or resize it into a different logo system unless the user explicitly requests a brand change.
- Public navbar may show the existing brand tagline through `BrandWordmark showTagline`. **Dashboard/app workspace headers MUST NOT show the marketing tagline; dashboard brand anchor is wordmark-only.**
- Never introduce legacy customer-facing brands such as Citin or DC Wedding.
- Never reintroduce wedding-only assumptions into general event workflows unless the selected event category specifically requires them.
- Preserve `/dashboard` and Beranda.
- Follow **Extend Over Replace**: inspect and extend existing routes, APIs, components, schema, and data flows before replacing anything.
- **Template catalog is one registry, not page-local arrays:** when adding a render-ready invitation design, register its key, category, preview capabilities and Studio preset once in `lib/templates/catalog.ts`. `/api/templates`, `/template-design`, `components/DigitalInvitation/TemplateSection.tsx` (on `/d-invitation`), and Invitation Studio must all consume that same catalog through `lib/templates/use-template-catalog.ts`. Never restore hard-coded marketing featured cards, standalone gallery categories, or a second Studio template list. A newly published designer upload is visible automatically as image-preview-only in public galleries and in Studio as a disabled item, but MUST NOT be selectable/published as a runnable invitation until its renderer and shared feature contract have been integrated into the ready registry. Public previews are available without login; Studio/editor and event data stay behind the existing server-side Dashboard authentication guard. A public CTA goes to the authenticated Dashboard/event setup first, not `/dashboard/editor` without an `invitationId`.
- **Three featured invitations on /d-invitation (22 September 2026):** The marketing Template Collection shows at most 3 READY templates from the shared catalog, ranked by count of unique event Payments with status PAID and a Digital Invitation package against the event\u2019s CURRENT `Invitation.templateKey`. Never invent sales or label random results bestsellers. Fill empty slots with randomly selected ready themes, and randomize all three before any paid adoption or if the aggregate database is unavailable. Fetch only public template keys through `app/api/templates/featured/route.ts`, keep actual customer/order data private, and do not modify the full public `/template-design` or Studio catalog. The highlight phone card must resemble the existing Hero mockup: aspect ~9:19.5, rounded metallic bezel, side buttons, notch, real lazy-loaded invitation preview; do not change Hero/Pintu/landing. There is no immutable template purchase snapshot yet; do not claim historical per-template sales based on current assigned keys. Keep the Template Collection header/title/description and right-hand full-gallery CTA grouped toward the center of the content frame (shared content width capped at 1100px, with responsive spacing), instead of pinning the copy and button to opposite frame edges. This constraint applies only to the collection header; preserve the three phone previews, hero composition and other marketing sections. The `Lihat semua template` / `Explore all templates` CTA uses normal text case and normal letter spacing, not forced uppercase/tracking. For this section, the underline/divider below the header spans the entire collection content width while the header copy and right CTA remain grouped in their shared centered max-1100px content wrapper; keep a visibly generous divider-to-smartphone gap (about 56px mobile, 64px desktop). The smartphone itself links to the preview, so do not render a redundant preview button below any phone card.
- **10 visually distinct built-in templates, exactly 5 photo + 5 photo-free (22 September 2026):** Current manifest `lib/templates/catalog.ts` must expose photo themes `romantic-rose`, `eternal-blossom`, `modern-maroon`, `garden-light`, `midnight-romance` and nonphoto themes `botanical-ivory`, `classic-pearl`, `golden-art-deco`, `paper-cut-botanical`, `celestial-ink`. Do not satisfy visual distinctiveness with only alternate palette, font and border radius. Each theme must have separately art-directed digital folded envelope and Cover, section ornamentation/layout, and honest mode-specific Gallery/Media; reuse core engine, security, RSVP, photo resolver and 13-section data contract. Declare `usesPhotos` and supported `photoSlots` in the one catalog, not in page-local arrays. Nonphoto themes MUST NOT render or ask for customer photos on cover, identity or gallery; retain the event's library intact when switching themes. Theme selector and public gallery must show actual lazy real template scenes with visible With-photo/Without-photo identification and filter, not repeated stock thumbnails. Existing 7 template keys remain stable for already-saved invitations; only add 3 new keys. See canonical PRD §7.2.3b. Keep `/d-invitation`, landing Pintu and protected assets unchanged.\n- **All ready invitation templates MUST have digital envelope + 13 real sections (22 September 2026):** Before Cover/Hero, show an interactive themed digital envelope with a working "Buka Undangan" control. After opening, include the semantic section contract Cover/Hero → Introduction/Greeting → Identity/Host/Couple → Event Detail → Date & Time → Gallery/Media → Countdown → Location/Maps → RSVP → Wishes → Gift/E-Angpao → Closing → Footer. Template presentation (frames, color, font, animations, positioning and section order where manifest allows) may vary; shared photo roles, content/feature behavior, and RSVP storage cannot be rewritten per template. Keep RSVP/Wishes/Gift ON/OFF controls; preserve structural section with honest empty states if a customer has not provided gallery/media, map URL, or bank details. Never invent customer content. Public/Studio/gallery must render the SAME real template component, not Studio-only fake form/screens. Public RSVP uses the shared functional form, preview RSVP NEVER submits to customer database, and Wishes must not imitate working persistence until the shared API exists. Route all published invitation URL variants through the shared renderer dispatcher. Unintegrated designer uploads remain image-preview-only. For all demo/catalog/default photo references, use existing local public assets (`/couple.jpg`, `/couple2.jpg`, `/couple3.jpg`, `/man.jpg`, `/female.jpg`) rather than external Unsplash URLs; those are NEVER a fallback replacement for real customer images. See canonical `prd.md` §7.2.3a. Do not change the separate landing Pintu when adding invitation envelopes.\n- **Shared customer photos / reusable template slot contract (22 September 2026):** Invitation Studio owns one event-scoped photo library; never duplicate uploads, database columns, APIs, or image pickers for every template. A template declares the supported roles in the single catalog (`photoSlots`: `cover`, `personOne`, `personTwo`, `gallery`). Store chosen `InvitationAsset.id` values, gallery selection/order and safe photo focus (top/center/bottom) as event-scoped design state via `lib/templates/photo-slots.ts`; resolve only image assets of the current invitation in both preview and public renderers. Template presentation owns geometry, frame, crop, animation and visual placement; slot names are shared, coordinates are NOT hardcoded globally. Legacy designs with no assignments retain existing fallback photos; old media URLs remain valid. Studio should allow photos to be assigned from one collection and from direct clicks on eligible canvas slots. Never claim a slot is supported if its renderer ignores it. New invitation image binary uploads MUST go through the authenticated `/api/invitations/assets/upload` Sharp decoder/converter and be persisted as actual optimized `.webp` files in event-scoped folders; external URL-only image creation must not bypass conversion. Designer preview image uploads must also be transcoded to WebP. Do not bulk-convert old customer media or touch unrelated audio or receipts without an explicit migration.
- Database is the source of truth; do not introduce fake/mock invitation data.
- Authorization is server-side; UI entitlement must reflect server entitlement.
- Published Rangkaian Acara is customer-immutable: once `Invitation.isPublished = true`, server APIs must reject event-detail edits, unpublish attempts, and event deletion. UI must also stop exposing Edit/Hapus for that event. This lock applies to Rangkaian Acara metadata/lifecycle and must not silently disable unrelated Studio capabilities unless the PRD explicitly expands the lock.
- Existing routes and compatibility aliases must remain when required by the PRD.
- **Pintu is a core landing-page navigation surface. Do not remove or bypass it without explicit user instruction.**
- **Landing door-to-page transition: no second door (22 September 2026):** Keep the existing clickable, orbiting service doors and their normal closed/open selection artwork. Only after the user presses `Masuk`, the selected door's `PortalWorld` service image fades into a plain image-free Rose-lit passage; never show a second door picture, duplicated leaf/frame, distant orbital door or a new door asset inside the transition. `PortalCamera` must stay in front of the existing doorway surface (do not fly behind its flat texture into the other orbiting doors). Begin the shared `dc-portal-start` Rose veil partway through the existing zoom, and navigate only after zoom/veil have covered the original door. Keep the shared `PortalTransition.tsx` image-free, preserve destination routes, music/sound, reduced-motion, selection/opening behavior, and all landing composition/content outside the entry transition. This is a scoped fix to `components/Landing/Pintu/SimpleDoorLab.tsx`, not approval to replace approved landing doors or touch `/pintu-lab`.
- **Pintu 3D V2 (active):** Read `pintu3d.md` Rebuild V2 and `prd.md` §15.4.1a before touching Pintu. The uploaded original `public/pintu1.png` locks frame/crown/leaf-panel/handle/relief shapes: faithfully turn that into real Three.js mesh geometry, NOT a sliced moving facade or generic CSS/SVG flower icons. `/pintu-lab` is the V2 Three.js preview; `/pintu-lab/css` archives V1. Keep whole moving leaves and attached sculpt as children of one pivot per jamb; frame, crown and interior remain fixed. Every visible milestone needs purpose, changed files, actual CI status, and owner screenshot verdict written to `pintu3d.md` and Appendix A of `prd.md`. Do not promote to `/`, change orbital, or replicate three doors before Pintu 1 passes owner visual review.
- **Rose petals in `components/Layout/RosePetalBackground.tsx` are a protected visual element. Do not alter their behavior, appearance, count, timing, or styling unless explicitly instructed.**
- **Landing asset resilience (23 September 2026):** The approved four portal images (`/eventplanner.png`, `/Idigi.png`, `/guestbook.png`, `/Ufisik.png`) are local `public/` files, not remote/stock placeholders. The image plane in `SimpleDoorLab.PortalWorld` must load textures with an error callback so a missing/broken local PNG cannot throw from a Suspense loader and crash the entire Canvas; the existing plain Rose portal plane remains a temporary fallback while loading or when unavailable. Once the actual image loads, preserve its existing map, UV, selection, opening, fade and transition behavior. Diagnose an inaccessible image with its exact case-sensitive `public/` filename and HTTP response before redesigning the door. The approved `LandingFloralGlow` keeps both `flower.png` images at the same position, scale, mirroring and animations; supply explicit Next/Image dimensions with CSS fill-like sizing instead of `fill` that can warn about a zero/static parent. `SimpleDoorLab` must set `<Canvas shadows={{ type: THREE.PCFShadowMap }}>` instead of `shadows` boolean: the currently installed React Three Fiber may default to the removed `PCFSoftShadowMap` and spam warnings. Keep shadow rendering enabled and preserve existing light strengths/door meshes/camera/transitions. `THREE.Clock` deprecation is nonfatal and may come from React Three Fiber's internal Canvas clock constructor; replacing an app `useFrame` clock read does not remove a library constructor warning. Do not globally update Three/R3F, suppress console warnings or change the owner's approved Pintu animation solely to hide it; only revisit after a tested upstream-compatible release.
- **Canonical landing ambient accents:** owner explicitly approved promoting only the `flower.png` botanical layer and soft Rose glow from `/jiplak` into `/`. Keep them additive behind the existing landing composition. Do not use this approval to change Pintu, copy, layout, header/footer, rose-petal behavior, or other landing elements.
- **Reusable public marketing ambience and controls (22 September 2026):** Reuse `components/Layout/PublicMarketingAtmosphere.tsx` (the existing `LandingFloralGlow` flowers + `WindRosePetals` animation) on marketing pages; do not mount a second petal/floral instance on framed landing or `/d-invitation`. The persistent single-player `MarketingAudioProvider` in root layout owns music state across marketing navigation; render controls with `MarketingFrameFooter` inside framed pages and `MarketingFloatingControls` on unframed marketing pages. Keep the Instagram action in the bottom-right via `MarketingInstagramLink`. Do not add decorative audio/florals over dashboard, admin, customer editing/checkout, or real guest invitations by default; respect manual mute, autoplay restrictions, and reduced motion. Keep existing landing appearance and Pintu intact.
- **Framed template gallery visual baseline (22 September 2026):** `/template-design` MUST match the approved framed landing/`/d-invitation` outer shell: one reused `PublicMarketingAtmosphere`, embedded `Navbar`, embedded `MarketingFrameFooter`, centered 90vw Rose-bordered frame, ~80vw desktop inner content, and center-only scrolling with persistent header/footer. Global `PublicAtmosphere`, `PublicContent`, top-level Navbar/Footer and `MarketingFloatingControls` must recognize `/template-design` as framed to avoid a second flower/petal/audio/Instagram/navbar/footer layer. Keep modal previews outside the clipped frame, preserve the real unified template catalog, no-login public previews and protected Studio entry. Reuse Cinzel/Fauna One/DM Mono, light/dark and ID/EN, and avoid standalone white/full-page gallery UI. Visual alignment on this route does not authorize editing approved landing, Pintu, or `/d-invitation`.
- **Global Rose pill control system (22 September 2026):** Application-level single-line inputs, selects, filter chips, dropdown triggers/options and CTA buttons share the same fully rounded (pill) geometry, visible Rose outline, and consistent hover/focus in Light/Dark. Tokens live in `app/globals.css` (`--dc-control-radius`, `--dc-control-menu-radius`, `--dc-control-outline`); consume the global `components/ui/control-styles.ts` for filter, search and custom dropdown panels/options, and canonical `components/ui/button.tsx` / `components/ui/input.tsx` for Buttons/Inputs. Do not create new route-specific control-styles files or hardcode individual `rounded-xl`/neutral-border control defaults. Native `<select>` popup rows are OS-rendered and cannot guarantee rounded appearance; use a custom accessible dropdown with `controlStyles` when a rounded popup is needed. Existing approved navbar chrome, illustration/door, invitation-template artwork, icon-only controls, checkbox/radio, multi-line textarea and layout-specific geometry are intentional exceptions. Keep `/template-design` photo/category filter, search, sort A–Z/Z–A, preview and ID/EN behavior; do not restyle unrelated approved landing artwork while making the controls consistent.

## 3. Typography

Use only these application UI fonts:
- **Cinzel** — display, headings, titles, branding, editorial elements. **DC Organizer wordmark always uses the canonical `--font-dc-heading` token and must not be swapped to another font class.**
- **Fauna One** — body/UI copy, navigation, forms, buttons, descriptions.
- **DM Mono** — metadata, codes, timestamps, status values, technical labels, small utility text.

Do not introduce additional fonts, random Google Fonts, template fonts, or intentional browser/system fallback styling. Invitation-template typography may remain dynamic when it belongs to invitation content itself.

### Wedding family order and invitation copy (23 September 2026)

- The wedding setup for EACH partner exposes one mutually exclusive radio selection: **Anak Tertua** (eldest), **Anak Termuda** (youngest), or **Anak Keberapa** (numeric). Only the numeric selection reveals an integer input (>0), required when that option is selected. A previously saved numeric order must reopen with numeric selected. An untouched legacy wedding with no order remains optional/blank.
- Keep canonical `Invitation.groomChildOrder` / `brideChildOrder` as nullable numeric fields. Store the additional mutually exclusive mode in `groomChildPosition` / `brideChildPosition`, limited to `ELDEST`, `YOUNGEST`, `NUMBER` or null; NEVER pretend youngest has a knowable numeric sibling position or encode mode with negative/sentinel numbers. Preserve old data and the event's publish lock. An eldest/youngest selection clears the numeric order.
- The single `weddingParentLine` formatter in `lib/events/parents.ts` owns family copy for Dashboard preview, Studio preview and EVERY ready public invitation theme: `Putra/Putri Sulung`, `Putra/Putri Bungsu`, or numeric Indonesian ordinal; render **Title Case** including `Dari Bapak Chandra & Ibu Juni`. This transformation is display-only; never mutate saved names or alter narrative descriptions.
- Keep parent data optional (no dummy labels if either parent absent); public and Studio render the same saved wedding information. After schema edits, run `pnpm db:deploy` against the intended database and `pnpm db:generate` before running the app. CI must regenerate Prisma, test the shared formatter and build.

### Title Case for visible names and headings (23 September 2026)

- **Every word in a customer-facing name or standalone UI title starts with a capital letter.** Apply Title Case consistently to page titles, section/frame/card headers (including large Dashboard frames), navigation names, metric titles, and displayed person/event names: `Manajemen Tamu`, `Daftar Undangan`, `Nama Di Amplop`.
- Keep official spelling of brands, acronyms and product names, e.g. `DC Organizer`, `RSVP`, `VIP`, `VVIP`, `WhatsApp`; preserve numerals, punctuation and ID/EN meaning. Do not force uppercase on every letter, translate proper names, or apply Title Case to complete sentences, descriptions, helper text, message bodies, URLs or code identifiers.
- For user-entered names/event titles, use **display-only capitalization**; do not silently overwrite data in Prisma, API payloads, RSVP, messages or personal invitation storage. Reuse the Dashboard's scoped title/name display convention (`dc-ui-title` / `dc-ui-name` / `dc-ui-label`) for dashboard labels outside semantic heading tags. Ensure new UI headers in both ID/EN follow the same rule, without changing the approved landing/door or invitation-template artwork as a side effect.
- **Dropdowns (23 September 2026):** Title Case applies to all standalone select/option labels in Dashboard and operational forms, including RSVP sorting, event scope, guest/WA Blast recipients, category/tag filters, and public RSVP status choices. CSS scopes the selected value and native options, but browser/OS popup options may ignore styling: pass actual display strings through `lib/text/display-title-case.ts` for dynamic/translated choices and write static public RSVP choices in Title Case. Keep `option.value`, data matching/filtering, persisted names, phone numbers, URLs, emails, technical timezone IDs, and narrative/helper copy unchanged. Preserve canonical acronyms and brand spelling (RSVP, VIP/VVIP, DC, WA, WhatsApp).
- **Owner logout:** The Owner Panel must expose a visible Logout action in its header on desktop and mobile; reuse the authenticated `POST /api/auth/logout` session invalidation through `components/Auth/SessionLogoutButton.tsx`, shared with Admin. Navigate to Login only after the server confirms success; show a retryable error if logout fails. Never replace logout with a client-only navigation or expose any owner data to a signed-out session. No schema changes are needed for these UI fixes.
- **Data-form select chevron spacing (23 September 2026):** The right-side dropdown arrow inside Dashboard/Owner/Usher **data-entry select fields** needs a comfortable ~18px inset from the control's right border; `app/globals.css` provides one scoped Rose SVG chevron with sufficient text padding and stable appearance on hover/focus/disabled. Do not reposition the already-approved navigation/account menu arrows. Selects with a separate, deliberately positioned icon (e.g. `EventScopePicker`) and compact hour/minute selects opt out using `data-dc-native-chevron="true"` to avoid duplicate icons or squeezing time values. Keep values, option text, accessibility, responsive layout, and invitation artwork unchanged.

## 4. Canonical Theme
### Desktop View

- Always !! use 80% VW for primary desktop header/content/footer containers. Do not regress page-level content to a fixed `max-width: 1400px` (or similarly narrow legacy wrapper) that leaves excessive unused desktop space.
- Dashboard chrome may span the viewport, but the customer-facing header/content workspace should target `80vw` and remain capped by the available pane width so the sidebar never causes horizontal overflow.
- **Dashboard visual consistency is mandatory:** Beranda defines the operational visual language across customer pages: warm pale-Rose canvas (#fff5f7 Light Mode), neutral white/dark card surfaces, stronger Rose navigation and actions, shared fonts/controls, readable metrics and compact functional tables. Keep the public landing and Pintu unchanged.
- **Dashboard readability:** use the generous desktop workspace. Routine dashboard body/form/table copy should generally read around 14–16px, small metadata around 11–12px, section headings around 20–24px, and metric values around 24px. Do not shrink operational copy to 8–10px merely to make the UI look “clean”.
- **Dashboard sidebar contrast (23 September screenshot revision supersedes the older neutral rail):** Customer sidebar and brand block use a solid DC Organizer Rose background with white navigation; Dark Mode uses a deeper Rose rail. Hover and active are separate shades; only the selected item has curved-outside corner geometry and adequate gaps prevent adjoining hover colors from colliding. Dashboard header utilities follow restrained landing-navbar controls; never restyle the approved public navbar.
- **Dashboard access before Publish:** Digital Invitation payment must not hide RSVP, Guest Management, Personal Invitation, or other preparation workspaces. Users may explore and prepare them before Publish; the Digital Invitation payment gate belongs to the Publish action. Separate product entitlements such as WA Blast quota and Guestbook/Usher remain independent.
- **Dashboard bilingual + theme contract:** all customer Dashboard tabs/components must work in Light and Dark mode and support Indonesian + English through the existing shared providers. Indonesian is the default locale. Translate application copy/state, never user-owned event/guest/content data. Theme and language controls must remain reachable from the Dashboard header, including mobile access.
- Reuse/extend `components/Dashboard/DashboardPrimitives.tsx` for new dashboard surfaces, metrics, notices, and section structure instead of creating another dashboard visual primitive.
- Tables/graphs are encouraged when they expose real stored/derived product data; never manufacture dashboard metrics merely to fill space.
- Do not use decorative sequence numbering in customer-facing page/component copy (for example `Workspace / 01`, `Acara 02`, `Undangan 03`, numbered feature labels, or numbered cards). Use descriptive labels instead. Numeric values that are actual product data remain allowed and required where meaningful, including dates, times, prices, counts, capacities, quotas, child order, phone numbers, and metrics.
- Public burger menu omits a dedicated Home/Beranda item; the logo remains the home path. In Indonesian, service labels are `Perencana Acara`, `Undangan Digital`, and `Buku Tamu Digital`. `Layanan` remains a submenu parent; **only signed-out visitors see `Masuk` and `Daftar`**, while authenticated visitors see one `Dashboard` link in their place (no registration link), and service/package icons remain distinct.
- **Locked public navigation + auth visual baseline:** The public burger keeps its current geometry, menu/submenu structure, motion, neutral/light control surfaces and **Rose label/icon text in both themes**. Do not restyle these components while implementing legal/content/auth features. Login/Register keep the same shared white dialog surface in Light and Dark Mode, with subtle Rose radial glow, readable dark form text, Rose accents and pill controls from `components/Auth/auth-styles.ts`. Never replace with theme-dependent dark cards, strong pink backgrounds or black burger labels without the owner's explicit request.
- Registration dialog headers must reserve the top-right area for the close `X`; do not place a `Masuk` action there. Put the login switch below the registration form. Auth copy must remain general-event oriented.
- **Burger menu visual preservation and Rose text (22 September 2026):** Changes to Login/Daftar must not redesign burger navigation. Preserve dropdown position/spacing, menu/submenu structure, animations and original visual element types: Login uses the styled `Link` treatment; Daftar uses the shared `Button` treatment. All item labels and icons should use Rose `text-primary` and `dark:text-primary` by default; do not restore the conflicting `text-foreground` / `dark:text-foreground` on `itemClass` or let Button's base `dark:text-black` override Daftar. Only intercept auth activation to open the root dialog, leaving Navbar/Pintu intact. **Login/Daftar popups use a white surface in BOTH themes**, with only a subtle pink radial glow; use explicit dark text, muted-helper, input and Google-button colors so the white panel stays readable in Dark Mode. The backdrop may be Rose-tinted and translucent to let the existing landing flowers/petals show without rendering another atmosphere layer.

- **Public navbar session-dependent links (23 September 2026):** In the existing burger used on `/` Home and other marketing/public pages, check the **current server session** on each open using `/api/auth/session` with `cache: "no-store"`; use an inert loading placeholder until the result is known to avoid briefly showing Masuk/Daftar to authenticated users. When logged out, preserve the exact existing `Masuk` Link and `Daftar` Button/dialog behaviors. When logged in, replace both with exactly one styled `Dashboard` Link (in ID/EN) using the role destination from `lib/auth/dashboard-route.ts`: `USER` -> `/dashboard`, `OWNER` -> `/owner`, `ADMIN`/`FINANCE` -> `/admin`, `DESIGNER`/`EDITOR` -> `/designer`. Never redirect all roles blindly to `/dashboard` or show a Register action to existing sessions. Preserve burger spacing/animation, Rose labels, the login/register popup visuals, services and landing/Pintu. No client-side localStorage auth flags; logout is reflected by the next server session check.
- Landing 80vw should read as one editorial composition: the copy column may widen/grow downward, a quote/proof block may sit above a subtle separator, and muted capability checks sit below it.
- Never fabricate customer testimonials, customer names, ratings, or quotes. Only attribute a quote to a customer when a real source is available; otherwise use clearly non-customer brand/service copy.

### Brand Anchor
- Logo / brand primary: `#C07A84` Rose.
- Supporting rose: `#D9A3AA`.
- Deep hover/pressed rose: `#A65E69`.
- All application accent colors must stay within this Rose family unless a semantic status color is required.

### Light
- **Background:** `#FFFFFF` pure white.
- **Headings, icons, buttons, links, help chat, menu, and accents:** Rose `#C07A84` as the canonical brand accent. Supporting Rose `#D9A3AA` and Deep Rose `#A65E69` may be used for states and emphasis.
- **Primary text:** `#111111` near-black.
- **Secondary text:** black with opacity, preferably the semantic muted token, rather than introducing another text color.
- Surfaces should stay white; do not use pink as a page background or repeated card fill.

### Dark
- **Background:** `#0B0B0C` almost black.
- **Headings, icons, buttons, links, help chat, menu, and accents:** Rose `#C07A84` as the canonical brand accent. Supporting Rose `#D9A3AA` and Deep Rose `#A65E69` may be used for states and emphasis.
- **Primary text:** `#FFFFFF` white.
- **Secondary text:** white with opacity, preferably the semantic muted token, rather than introducing another text color.
- Surfaces should remain near-black/dark neutral; do not use pink as a page background or repeated card fill.

### Neutral / Accent Balance
- The visual goal is a white or almost-black canvas with Rose concentrated on meaningful brand elements: headings, icons, buttons, links, menus, help chat, selected states, outlines, and small accents.
- Treat 60/30/10 as a visual-balance principle, not literal pixel coverage. Do not force large pink areas merely to satisfy a ratio.
- Do not use Rose as a large decorative glow, repeated card fill, or default body-text color.
- Text hierarchy should come primarily from typography, spacing, layout, and opacity.

### Navbar Control Standard
- Public-navbar controls use the same restrained opacity treatment established in the current implementation; do not increase their fill opacity or redesign them into solid/pill-heavy controls without explicit user instruction.
- Theme toggle, language selector, and burger trigger should read as one control family through consistent dimensions, borders, spacing, and subtle Rose hover treatment.
- The public navbar itself stays visually continuous with the page canvas; do not add a contrasting navbar block merely to mask background effects.

### Button Color Standard
- **`components/ui/button.tsx` is the single canonical application button primitive. All reusable application buttons MUST render through `Button`; do not introduce or maintain separate visual button primitives.**
- The button has **one visual treatment only**. There are no visual variants. `size` is only a sizing axis and must not change color, border, shadow, gradient, or shape language.
- Canonical shape: fully pill-rounded using global `--dc-control-radius`, with a visible Rose outline, and solid Rose `#C07A84` fill; hover/pressed state may use Deep Rose `#A65E69` or Supporting Rose `#D9A3AA`. Shared `Button` and `Input` must follow the same global control contract. Special-purpose nav/icon/illustration controls may retain their previously approved geometry.
- Canonical depth: subtle raised shadow only. **No gradients** and no page-specific glow/shine effects.
- Light mode button text is **white**. Dark mode button text is **black**.
- Legacy `variant` values are accepted only for source compatibility and MUST resolve to the exact same visual treatment.
- Do not introduce one-off button colors, gradients, borders, shadows, or page-specific button variants outside `components/ui/button.tsx`; generic filter/dropdown/field control styling belongs to the global `components/ui/control-styles.ts`.
- Approved exception: burger navigation keeps its existing neutral/light surface with Rose labels/icons; auth dialog fields/Google action use the shared white-and-Rose treatment, while their main submit continues to use the canonical shared `Button` CTA. Do not extrapolate this exception into restyling the burger or forms.

## 5. Anti AI-Slop Text Hierarchy

- Do not color every piece of text differently.
- Functional/body text should remain the primary readable text color.
- Rose is an accent for the specified brand elements, not a replacement for body text.
- Establish secondary hierarchy through opacity, typography, weight, size, spacing, layout, and contrast first.


### Dashboard microcopy

- Customer-facing Dashboard copy must be concise, natural, and context-aware. Do not repeat a page/category noun merely to fill an eyebrow, title, description, selector label, metric, card, or action.
- Avoid stacked repetition such as `Workspace → Acara → pilih acara → acara aktif` when the current screen already supplies that context. Keep a term only when it disambiguates scope, state, or the next action.
- Prefer removing redundant copy over replacing it with a synonym. A shorter label such as `Total`, `Terbaru`, `Lihat semua`, `Detail`, or a context-specific eyebrow is better when the surrounding UI already names the object.
- Generic decorative labels such as `Workspace` must not be used as filler. Use a meaningful context label such as `Persiapan`, `Publikasi`, `Distribusi`, `Kehadiran`, or omit the label when it adds no information.
- Do not duplicate the same metric/context in adjacent header cards and metric grids. If the information is already clearly visible nearby, remove the duplicate.
- Small owner corrections to recurring UI/copy style are repository conventions, not throwaway tweaks: capture them in this file and mirror active requirements/history to `prd.md`; when the owner explicitly asks for the supplemental log, also update `prd-tambahan.md`.
- Landing root chrome must visually merge with the landing canvas: header and compact landing footer use `var(--background)` rather than a visibly separate surface. In landing Dark Mode, the selected ID/EN option uses opaque Rose with near-black text, and the theme toggle uses opaque Rose with a near-black icon/text. Scope these landing-only overrides through the landing navbar class so Dashboard/shared controls are not accidentally restyled.

## 6. Motion & Accessibility

- Animated files must use `"use client"` when required by the framework.
- Animate transform (`x`, `y`, `scale`, `rotate`) and opacity whenever possible; do not animate layout properties such as width, height, margin, or padding for routine motion.
- Button interactions: soft hover scale around `1.02`, tap scale around `0.98` where appropriate.
- Interactive cards: vertical lift around `y: -4` with spring physics.
- Natural entrance easing: `[0.22, 1, 0.36, 1]`, normally `0.4s–0.6s`.
- Interactive spring: `stiffness: 400`, `damping: 25`.
- Scroll reveal uses `whileInView` with `viewport.once`.
- Marketing `/d-invitation` uses the approved landing-style viewport frame: embedded navbar and compact footer remain fixed within the frame, all marketing sections scroll inside its central panel, and per-section `whileInView` reveals must observe that inner scroll root (not the document viewport). Preserve the existing landing `/` scene and honor reduced motion.
- **Digital Invitation marketing section rhythm, Studio CTA, package and FAQ (22 September 2026):** On `/d-invitation`, use one consistent page-level gap between Hero, Features, Template Collection, Design Studio, Package, Reviews and FAQ (mobile `gap-20`, desktop `gap-24`); do not stack oversized Hero bottom padding with parent margins. Keep section-internal header-to-content spacing restrained without changing the approved three smartphone collection gap. Group Studio copy and CTA near the center of the shared max-1100px section (compact, centered two-column group with responsive gap), with the action label `Masuk Studio` / `Enter Studio` plus arrow, linking to `/studio` rather than always to Dashboard. `/studio` must require login and a configured owned event; one event goes straight into its editor with an invitationId, multiple offer an event choice, no event offers event creation, never open an unauthenticated/ID-less editor. Package card on this route has a generously round (40–48px) Rose outline via `PackageShowcase.roundedCard`, not a global redesign of other service package cards. All reusable FAQ accordion cards and clickable question surfaces are rounded, with Rose outline; keep keyboard and expanded-answer behavior. Preserve existing landing, Pintu, marketing effects and real invitation renderers.
- **Unified /d-invitation width & repeatable text motion (22 September 2026):** Without editing the ID/EN content, share a single centered `max-w-[1100px]` responsive content column between Hero, Features, Template Collection, Studio, Package, Reviews, and FAQ; page width stays at 88% on mobile and 80vw from `sm` up, within the marketing frame. All section borders/header/grid/FAQ outer edges follow the same bounds; reading-width paragraphs, the centered Studio copy+CTA pair and individual package/phone cards may remain compact inside that column. Keep the established 80px mobile / 96px desktop section gaps and 56px / 64px template divider-to-phone gap. `MarketingTextReveal` must observe only the main frame’s inner scroll panel using IntersectionObserver and apply gentle opacity / vertical transform to text without changing strings, layout dimensions, buttons or phone preview animations. Reset an individual text reveal **only after the text actually leaves that visible scroll panel** and replay when it re-enters from either scroll direction; do not use `once:true` for text. Preserve existing section PuzzleAssemble entry behavior, no text splitting/cloning, and respect `prefers-reduced-motion`.
- **Event Planner framed marketing styling (22 September 2026):** `/event-planner` shares the already approved `/d-invitation` viewport frame (Rose outline, embedded navbar/footer, internally scrolling main panel, single shared floral/glow/wind-petal background with persistent marketing music and Instagram) and centered max-1100px responsive content width (88% mobile / 80vw above `sm`), with 80px mobile / 96px desktop section spacing. Update root-layout `PublicAtmosphere`/`PublicContent`, global Navbar, Footer and `MarketingFloatingControls` route checks so the Event Planner frame never stacks duplicate public navigation, footer, background or floating controls; do not change other marketing routes. Keep the existing Event Planner text, actual founder/service/portfolio/package/review/FAQ content, WhatsApp consultation links and button behavior. Use branded Rose headings and subtly rounded Rose-outline content cards without introducing decorative numbering or fake metrics. `EventPlanner/ScrollReveal.tsx` repeats subtle opacity + translateY on section re-entry using the frame's scroll root and `once:false`; the existing `DigitalInvitation/MarketingTextReveal.tsx` handles repeatable text motion, excluding interactive controls and respecting reduced-motion. Portfolio video modal must portal to document.body above the clipped scrolling frame. Keep approved landing Pintu and invitation page unchanged.
- **Digital Invitation feature grouping (22 September 2026):** On `/d-invitation`, the `Yang kamu dapatkan` / `What you get` section displays three descriptive feature cards without decorative 01/02/03 sequence labels. Group the section heading and feature grid together within the shared centered `max-w-[1100px]` page wrapper with consistent columns/padding so the cards do not scatter to the edges of the 80vw content area. Keep the existing feature icons, copy, section borders and mobile stacking; never reintroduce sequence numbers in this section.
- Digital Invitation marketing hero proportion: present the illustrative phone as a narrow, tall smartphone (approximately 9:19.5 with responsive width), allocate the larger desktop column to the hero copy, and allow heading/description to use that width. Center the **pair** within the main frame (desktop hero width shares the 1100px maximum content width) with a moderate gap so the copy and phone sit closer together instead of hugging opposite edges. Keep the existing phone content, scroll animation, portal arrival effect, main frame, and landing unchanged unless requested.
- Use `AnimatePresence mode="wait"` for tab/modal/page transitions where applicable.
- Always respect `useReducedMotion()` and remove heavy motion when requested.
- Motion must never reduce usability or obscure important content.
- Landing Pintu decorative ornament at the top belongs to the closed-door surface: it fades/moves out when that door is active/open and returns when the door closes. Keep this synchronized with the same `isActive` state as the door panels and respect reduced motion.
- While the temporary `/jiplak` experiment exists, visual experiments must stay isolated there. Do not wire experimental Pintu/background components into `app/page.tsx`, `PintuSection.tsx`, `PintuCard.tsx`, or `RosePetalBackground.tsx` unless the owner explicitly approves promotion to the canonical landing.
- The `/jiplak` WebGL/Three.js portal is an **independent, optional experiment**: load Three only after `/jiplak` mounts, retain its own copied DOM Pintu as a functional fallback when WebGL is unavailable, honor reduced motion and dispose GPU resources/listeners on unmount. Never import its engine into `/` or change the canonical landing's Pintu/flower/rose-petal behavior by implication.
- `/jiplak` 3D follow-up: preserve the copied Pintu's continuous three-door orbital Motion rotation (including pause/resume on hover), natural-image aspect ratios, readable real depth and soft ground shadow. Door/trim surfaces must stay in canonical Rose matte material rather than shiny plastic; no saturated pink portal halo, light spill, or large background glow without an explicit new owner request. This instruction is scoped **only to `/jiplak`**.

## 7. UI Components

Use existing Shadcn UI and Lucide React patterns. Customize Shadcn components using DC Organizer semantic theme tokens instead of untouched slate/zinc/neutral defaults.

Use Lucide icons consistently and keep interactive targets at least `44x44px`. Use `asChild` on `SheetTrigger`/`DialogTrigger` when wrapping custom trigger elements to avoid nested-button DOM issues.

## 8. Repository Structure & Naming

- Organize reusable business UI by feature with descriptive PascalCase folders under `components/`, for example `DigitalInvitation`, `EventPlanner`, `Guestbook`, `Dashboard`, `Payments`, and `Landing/Pintu`.
- Shared provider modules must be explicit: keep state/provider logic in `ThemeProvider.tsx` / `LanguageProvider.tsx` and UI controls such as `ThemeToggle.tsx` / `LanguageToggle.tsx` separate. Do not recombine provider, hook, and control UI into one context file.
- Reserve `components/Layout`, `components/Brand`, `components/Theme`, `components/I18n`, `components/Marketing`, and `components/ui` for shared/global concerns. Do not place feature-specific purchase, editor, or service components in `Layout`.
- Avoid ambiguous abbreviations or route-shaped names in component folders such as `D-Invitation` or suffixes such as `Page` when the folder already represents the feature.
- Active components must use stable semantic names. Do not keep `V2`, `V3`, `New`, `Old`, or similar version suffixes after a replacement becomes canonical; remove obsolete versions once repository references are verified absent.
- Static customer-facing content belongs under `data/<domain>/`: service content in `data/services/`, template showcase/catalog presentation data in `data/templates/`, and similar future domains in their own explicit folders. Route/page files should not own large static catalogs.
- Keep domain logic/helpers in `lib/`; do not move executable business logic into `data/`. Group focused helpers by domain (for example `lib/invitations`, `lib/security`, `lib/usher`, `lib/notifications`) instead of adding more unrelated files at the `lib/` root.
- Route folders under `app/` follow URL requirements and should not be renamed merely for code-style consistency.
- When moving a component/data file, update all imports in the same change and validate with the production build before merge.
- Split large feature components by responsibility when they mix orchestration/state, static configuration, domain types/helpers, and reusable presentation. Keep orchestration in the workspace/page component; move reusable panels, config, types, and pure helpers into adjacent feature files. Do not split purely to reduce line count.
- Do not split a component that already has a clear responsibility and manageable size. Prefer a stable cohesive file over artificial fragmentation.
- Event Panel structure is canonical: `EventPanel.tsx` owns event orchestration/API mutations and the tightly-coupled list/editor flow; `event-panel-types.ts` owns contracts; `event-panel-helpers.ts` owns pure form/date/time conversion and validation; `EventFields.tsx` owns reusable event editor field controls.
- Seating Chart structure is canonical: `SeatingChart.tsx` remains the cohesive canvas/editor orchestration layer; `seating-chart-types.ts` owns contracts and `seating-chart-geometry.ts` owns pure table/seat geometry. Do not split the canvas into many prop-heavy presentational components unless its responsibilities materially expand.
- Personal Invitation structure is canonical: `PersonalInvitationPanel.tsx` owns event-scoped loading, selection, and server mutations; `PersonalInvitationPanels.tsx` owns create/list/editor/password presentation; `personal-invitation-types.ts` owns contracts and `personal-invitation-helpers.ts` owns event sorting and public-link construction.
- WA Blast structure is canonical: `WhatsAppBlastPanel.tsx` owns event selection, quota loading, purchase, and recipient mutations; `WaBlastPanels.tsx` owns recipient form and queue presentation; `wa-blast-types.ts` owns event/guest/recipient contracts.
- Invitation Studio editor structure is canonical: `InvitationDesigner.tsx` owns orchestration/history/upload/save; `DesignerPanels.tsx` owns editor controls; `InvitationPreview.tsx` owns preview rendering; `designer-types.ts`, `designer-config.ts`, and `designer-state.ts` own model/config/state helpers.
- RSVP form structure is canonical: `RsvpForm.tsx` owns submit orchestration/state; `RsvpPanels.tsx` owns input/success presentation; `rsvp-helpers.ts` owns pure calendar/QR/ticket helpers; `rsvp-types.ts` owns the form/ticket contracts.

## 9. Code Quality

- TypeScript must remain type-safe.
- Prefer existing architecture and dependencies.
- Do not add a dependency when an installed package already solves the requirement.
- Keep components maintainable; avoid unnecessarily large files and duplicated logic.
- Follow existing Prettier/ESLint conventions and Tailwind class ordering.
- Avoid unused variables, invalid DOM nesting, stale hook dependencies, and client-only APIs in server components.

## 10. Data & Entitlement Rules

- Shared event data must be persisted and read from PostgreSQL/Prisma.
- Event identity is event-scoped. Couple-specific fields are required only for event categories that use a couple identity.
- Wedding same-day session invitation rule: for WEDDING only, allow optional Akad Nikah/Pemberkatan Pernikahan/Prosesi Pernikahan and Resepsi sessions on ONE eventDate with independent time/location. Different calendar days require separate event IDs and separate event-scoped Digital Invitation payments. A guest's session access must be saved per guest and enforced server-side/personal renderers; never interpret legacy `ceremonyTime`/`receptionTime` as two separate wedding sessions. Do not ship form-only fields without persistence, authorization, template/preview filtering, and migration validation. See `prd.md` §5.7.
- Legacy fields such as `groomName`, `brideName`, `weddingHashtag`, `WEDDING`, and `ADAT_AKAD` may remain for backward compatibility but MUST NOT be treated as universal product semantics.
- No placeholder couple names or fake invitation records.
- Digital Invitation entitlement is event-scoped; payment for one invitation/event must not unlock another event.
- **Paid invitation share QR (23 September 2026):** Every owner can reveal/download exactly one stable invitation-sharing QR per `Invitation.id` whose own `Payment.status=PAID` and `packageKey` grants Digital Invitation access; an unpaid event or a different owner's event MUST be rejected by `GET /api/invitations/qr?invitationId=...` on the server (not just hidden by the UI). Place `Buat QR` and `Download QR PNG` inside that invitation's row in the existing **large** Undangan Digital panel, without new nested cards. The QR encodes canonical application `APP_URL/q/<invitationId>` and is deterministic per event; `app/q/[invitationId]/route.ts` resolves the current public slug using a non-cached temporary redirect only after Publish + valid event/template + its own paid Digital Invitation entitlement. Thus QR can be prepared/downloaded after payment while a draft, but it cannot publicly open the invitation until published. Do NOT use/reuse `lib/usher/qr.ts` signed individual Guest check-in tokens for this owner/event QR, expose guest info in QR payloads, add a second duplicate QR per order, or make QR purchases account-global. The existing QuickChart QR image provider may only see the public app redirect URL, not a guest token. `APP_URL` must be an externally reachable canonical app origin in production (local `localhost` QRs are local only). Do not mutate the schema or add a new migration merely to store a QR image that can be derived from the event ID.

- Creating/saving an event and editing/saving a template may happen before payment. Payment is enforced when publishing according to `prd.md`.
- A public invitation requires a configured event, a saved template, published state, and valid event-scoped entitlement.
- Server-side access checks are authoritative.
- Guest management and seating mutations must remain server-authoritative and collision-safe.

## 11. Documentation Rule

At the end of every material implementation change:

- Update the canonical body of `prd.md` when active product requirements change.
- Append the implementation change, rationale, affected files/area, commit(s), and validation status to Appendix A of the same `prd.md`.
- Do not create parallel/split PRD files for normal implementation history.
- Never claim build, lint, CI, migration, or deployment success without an actual observed result.


## Approved landing-page baseline (21 September 2026)

The owner has approved the exact current `/pagecontoh` composition as the production landing page `/`. Treat the approved implementation as a **locked visual and interaction baseline**, not as a starting point for unsolicited redesign. The homepage re-exports `app/pagecontoh/page.tsx`; keep `/pagecontoh` available as the visual reference.

**Change-control contract:** Implement only what the owner explicitly asks to change. Do not add, remove, rename, reposition, restyle, replace, simplify, or "improve" unrelated content, assets, controls, effects, animations, links, sections, routes, or components. Do not interpret approval of one change as approval to modify other areas. When a necessary technical fix has a visible side effect, explain it and ask before changing the approved composition. Preserve existing working behavior and make the smallest scoped diff.

**Visual source of truth:** `app/pagecontoh/page.tsx` and its existing imported components, assets, classes, theme states, responsive behavior, and motion settings define the approved landing language. Future landing pages and related public marketing UI should reuse their established styling and existing libraries/components rather than inventing new card, button, color, typography, animation, background, or layout systems. This is a style reference, **not** permission to clone landing-only decorative elements onto every page or alter Dashboard/Invitation templates without instruction.

Approved landing composition: continuous floral/glow/petal scene; centered rounded main frame; embedded shared navbar and compact footer; four orbiting 3D service doors; two transparent rose-outlined cloud copy areas whose pieces assemble in place and whose bilingual text reveals letter by letter; audio controls and Instagram link. Light/dark styling and ID/EN behavior must remain consistent with the approved implementation. Preserve exact copy and positioning unless explicitly requested.

Implementation references: `app/page.tsx`, `app/pagecontoh/page.tsx`, `components/Landing/Pintu/SimpleDoorLab.tsx`, `components/Landing/CloudCopy.tsx`, `components/Landing/LandingFloralGlow.tsx`, `components/Landing/WindRosePetals.tsx`, `components/Layout/Navbar/Navbar.tsx`, `components/Layout/Footer.tsx`, `components/Layout/PublicAtmosphere.tsx`, `components/Theme/ThemeToggle.tsx`, `components/I18n/LanguageToggle.tsx`, and `components/ui/button.tsx`. Existing stack: Next.js App Router, React, TypeScript, Tailwind CSS, Motion (`motion/react`), Three.js and React Three Fiber where already used, and existing shared providers/components. No new dependency merely for a visual preference.

Before changing the landing, inspect the current source and compare the requested change against this baseline. Afterward check both `/` and `/pagecontoh` in light/dark and ID/EN where relevant; report honestly if browser/build validation was not performed. Earlier experimental restrictions against promoting `/pagecontoh` to `/` are superseded by the owner's explicit approval on 21 September 2026; this does **not** approve promoting the separate `/pintu-lab` GLB experiment or changing its reference requirements.


## Public marketing mini-door wayfinding (22 September 2026)

The owner explicitly requested a small door-shaped page navigator on the left of public marketing pages. `components/Layout/MarketingDoorNavigator.tsx` is mounted once from `app/layout.tsx` and must remain separate from the approved 3D landing doors, existing navbar/burger, PortalTransition veil, and dashboard sidebar. Keep the widget collapsed by default; the expanded rail shows actual route links, a mini-door visual, short destination descriptions and current-page indication with ID/EN copy. Preserve its mobile scroll handling, outside-click/Escape/close accessibility and reduced-motion support. Keep it off Dashboard, Studio, customer invitation URLs, and unrelated public pages. Use the shared Rose route transition; do not introduce a second Pintu graphic during page transitions. Do not modify approved landing doors/frame or page content when adjusting this widget.


## Guestbook and Printed Invitation framed marketing pages (22 September 2026)

`/guestbook` and `/undangan-fisik` follow the same approved 90vw Rose-bordered viewport shell as `/event-planner`: single `PublicMarketingAtmosphere`, embedded `Navbar`, embedded `MarketingFrameFooter`, internally scrollable `main`, and content capped at 1100px (88% mobile / 80vw at sm). Keep the root `PublicAtmosphere`/`PublicContent`, global Navbar/Footer and `MarketingFloatingControls` suppression in sync across both routes; do not mount duplicate petals, music or Instagram. Reuse `EventPlanner/ScrollReveal` and `DigitalInvitation/MarketingTextReveal` with the actual scroll root so sections/text replay on re-entry and respect reduced-motion. Maintain existing Guestbook functional tabs, package, reviews and FAQ; preserve Printed Invitation illustration, process and in-panel #proses/#konsultasi anchors, WhatsApp URL and digital-invitation link. The mini-door navigator remains present on these routes. Do not alter approved landing/Pintu, other service pages, Dashboard or customer invitations. Follow canonical `prd.md` §16.1.


## Shared music in all ready invitation themes (22 September 2026)

Every READY invitation template (five photo + five photo-free) must have a built-in default audio track from existing `public/` music, centrally mapped by its stable template key in `lib/templates/music.ts`. Event-specific saved `musicUrl` and owned uploaded AUDIO assets override the default; do not create per-theme media upload systems or duplicate audio files. All renderers reuse `components/PublicInvitation/InvitationMusic.tsx`: one audio element lives *outside* the conditional envelope branch; start in the actual `Buka Undangan` click/tap handler for public invitations, never by autoplay on page load or a delayed effect. Keep a reachable play/pause toggle after open, loop audio, stop on unmount/hidden tab, respect blocked-play browser behavior. Preview card/Studio/gallery must never start playing on open automatically; allow explicit play, prevent two invitation tracks playing together, and pause the existing marketing soundtrack during preview playback. Music controls work regardless of RSVP/Wishes/Gift section switches; do not repeat the old footer <audio> element. Studio's music URL/upload overrides and unsaved-preview flow must continue to work. Music never renders on locked/password-gated invitations, dashboard pages, or marketing-only surfaces. Ensure music files are licensed for the intended commercial/public use before a production release. Canonical contract: `prd.md` §7.2.3c.


## Public account entry styling: Login and Daftar (22 September 2026)

The owner requires the public `/login` page and the navbar's `RegisterDialog` to match the approved Rose visual system rather than hardcoded white/black, rectangular or neutral controls. Reuse `components/Auth/auth-styles.ts` and `components/Auth/GoogleIcon.tsx` for matching card/modal fields, Google action and primary submit across Light/Dark; use the shared Cinzel heading, Fauna One form copy, pill-shaped controls, Rose outlines, accessible focus/error states, responsive modal scrolling and ID/EN provider. Do not alter protected auth endpoints/role routing or fake account success. Both Login and Daftar must be presented as one persistent root-mounted auth dialog (outside burger), switching modes without route navigation; opening an auth modal closes the burger first. Existing `/login?register=1`, Google OAuth failure callbacks and protected-route redirects must remain valid backward-compatible links which redirect to `/?auth=login|register&next=...` and show that same modal with a safe relative `next` target. Login → Daftar → Login mode switching must not stack two dialogs; after registration show an email-verification notice in the login dialog. Root `PublicContent` makes only /login full-width so the mobile form fits; existing global navbar/footer and atmosphere stay single-instance. Avoid touching the approved landing Pintu, Dashboard auth/entitlement and unrelated UI. The auth card classes must not override `DialogContent` fixed positioning; keep register vertically centered on desktop, compact enough for ordinary viewport sizes, and scroll inside the popup on short/mobile screens. See canonical `prd.md` §§4.1a–4.1c.


## Public privacy policy and protected UI baseline (22 September 2026)

The owner explicitly froze the currently approved burger menu, Login popup and Daftar popup visuals. For unrelated work (e.g. publishing legal copy), **do not edit** `components/Layout/Navbar/BurgerMenuContent.tsx`, `components/Layout/Navbar/Navbar.tsx`, `components/Auth/auth-styles.ts`, `components/Auth/LoginDialog.tsx` or their layout/style contracts without a specific new request. Burger item labels/icons remain Rose; Login/Register backgrounds remain white with subtle Rose glow across both themes. This rule supersedes older neutral-white/black-burger-label or theme-dependent-auth-background descriptions elsewhere in documentation.

`app/privacy-policy/page.tsx` is the public Privacy Policy page based on owner-provided Indonesian copy, with the old service name replaced by DC Organizer and an English translation for the existing locale switch. `RegisterDialog`'s consent label must include real readable `/privacy-policy` and `/terms-and-conditions` links (both open a new tab so the registration form and unchecked checkbox state remain available), and existing non-embedded footer legal labels must link to their respective routes. The terms text exists at `app/terms-and-conditions/page.tsx` and is an adapted DC Organizer draft based on owner-supplied third-party wording; do not silently copy the original company's legal identity, address, domain, contact email, unsupported commission payout schedule, or sweeping perpetual IP/moral-rights assignments. Any changes to the legal text must be reviewed against actual DC Organizer operational policies. The policy text contains operational/legal claims (data sources, processors, sale/rental prohibition, contractual limits, one-day advance notice), which must be checked against implemented data flows, actual third-party terms and legal counsel before commercial production; do not silently invent further promises or assert that this content alone establishes legal compliance. Canonical requirements and changelog: `prd.md` §4.1e.


## Public terms and conditions (22 September 2026)

`/terms-and-conditions` is the public ID/EN terms page. Preserve the owner-provided source's core topical order: general terms; definitions; acceptable use; user content; fees; warranties/availability; responsibility/limitations; intellectual property; privacy; partner commission where applicable; other terms. This is a business-specific adaptation, not a verbatim substitution: do not represent legacy Viding/third-party company details, sweeping content-right waivers, an unverified minimum age, automatic location/IP-processing claims, or a 1–3-business-day partner withdrawal service promise as DC Organizer facts. User content rights remain with the user; license only what is needed to operate the chosen service. Do not assert that drafted terms override mandatory consumer/data rights. The registration consent must link to BOTH terms and privacy, in a new tab without toggling the checkbox, and the public footer terms link must resolve. Preserve *all* approved burger/login/register look-and-feel; only consent text markup or existing footer label links may change as explicitly needed. The text must be reviewed by the owner/legal adviser before production, especially business identity, age eligibility, package cancellation/refunds, content licenses, vendor/partner commissions, contacts and legal obligations. Canonical PRD: §4.1f.


## Google OAuth local configuration and safe error reporting (23 September 2026)

Google Login/Register buttons in the *existing locked white/Rose auth dialogs* navigate to `/api/auth/google`; they require the operator's own server-side `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, a running user/session database, and a valid `APP_URL`. For local testing on `http://localhost:3000`, use `APP_URL=http://localhost:3000` and register exactly `http://localhost:3000/api/auth/google/callback` as an authorized redirect URI in the Google Cloud **Web application** OAuth client. Do not assume blank values in tracked `.env.example` mean the user's private `.env.local` is blank; never retrieve/print/commit secrets, mock sign-in, or claim OAuth end-to-end success from CI alone. If the app is configured to use localhost as its OAuth callback, start in a browser using localhost rather than the dev server's advertised LAN IP: different hostnames have distinct cookies and Google redirect URI registration constraints. Restart the dev server after changing server env.

The auth start handler must validate *both* Google client ID and secret before setting transient OAuth cookies. The Google callback should expose only stable non-sensitive error codes (configuration, state mismatch, user cancellation, token exchange, verified profile, user/session persistence) to the existing LoginDialog; log root failures server-side without emitting tokens or client secrets. Maintain the one-dialog UI, the approved burger/auth visual freeze, the current consent links, state-cookie CSRF checking, valid same-site cookies, safe relative `next`, existing role routing, real session persistence and provider redirect. Avoid changing presentation classes merely to make error messages more actionable.


## Landing cloud text hierarchy (23 September 2026)

On `/` and `/pagecontoh`, `components/Landing/CloudCopy.tsx` owns the two existing outlined clouds. The top-left cloud should read as the primary opening with a larger, legible heading and body. The bottom-right cloud is the quieter continuation, modestly raised away from the footer. Keep original ID/EN copy, fonts, SVG silhouette, letter-reveal and departure/return motion, and the approved 3D door composition unchanged. Tune only responsive cloud widths, text sizes, spacing and bottom offset to avoid crowding or clipping. Do not redesign any other landing component as part of this scoped change. Visual check remains required on mobile and desktop in both languages.


## Shared brand wordmark across pages (23 September 2026)

Branding is **one reusable component**, `components/Brand/BrandWordmark.tsx`, which owns the exact name, Cinzel display typography, Rose color, tagline string, and responsive size tokens. All customer-facing visual brand lockups must render `BrandWordmark`, not custom text or manual two-line `D C / ORGANIZER` markup (including the regular public Footer). The default `size="public"` displays the same approved larger name (24px mobile, 34px from sm, 36px from lg) and optional larger tagline (8px mobile, 9px from sm, 10px from lg) in **every public Navbar**, not only `/` or `/pagecontoh`. The shared public Navbar uses `py-5 sm:py-4` for a similar overall height to the prior design, without moving the theme/language/burger controls or changing their appearance. The narrow workspace header/usher/mobile contexts reuse this SAME component with explicit `size="dashboard"` or `size="mobile"` to fit available space; dashboard does NOT show the public tagline. To adjust brand wording, typography, palette or shared responsive sizing, update `BrandWordmark` once rather than applying route-specific or footer-only overrides. Preserve unrelated layout/Pintu/doors/cloud/auth and test desktop and mobile fit. This supersedes the previous landing-only brand-size rule.


## Landing orbital scale and hover (23 September 2026)

Owner approved a **scale-only** change to the active landing `components/Landing/Pintu/SimpleDoorLab.tsx`: when no service is selected, orbiting doors use a slightly stronger depth scale (back z=-1.25 → 0.62; front z=+1.25 → 0.85, linear between); hovering an individual door adds a restrained 10% scale multiplier while the pointer stays over it. Hover is disabled for reduced-motion preference, when a door is selected, and during entry; selected-door sizes remain the established 1.12 active / 0.70 others. Keep the existing `MathUtils.damp(..., 3.8, delta)` scaling transition, phase speed, group x/z positions, camera, pivot/door-open motion, mesh, palette, service imagery, lighting and Rose portal transition exactly as before. No new hover glow, ornament, pause or cursor-driven rotation. The change does not authorize edits to `/pintu-lab`, `/jiplak`, clouds, navbar, footer, marketing pages, auth or customer templates. Check the relative sizes and any clipping/overlap in desktop/mobile at real browser viewport sizes; CI validates build, not visual fidelity.


## Seamless Rose portal glow (23 September 2026)

The approved landing door entry and marketing route transition use the persistent `components/Landing/Pintu/PortalTransition.tsx`. To avoid visible rectangular glow tiles during camera zoom, the veil is **one fixed full-viewport Rose radial-gradient surface** that fades in/out via opacity only (`app/globals.css` `dc-marketing-veil-in`/`dc-marketing-veil-out`). Never reintroduce a separately `transform: scale(...)`-animated viewport glow, a different opaque `clip-path` shape behind it, an image of a second door, or additional independently animated translucent boxes. Keep the established timings (`COVER_MS=780`, `DOOR_COVER_MS=950`, `REVEAL_MS=900`), existing sound, navigation/pending-route/reduced-motion behavior, and destination `dc-marketing-reveal` event. Do not modify the approved 3D door geometry, orbit and hover sizes, open animation or camera flight: this change is limited to transition compositing. Route animations and visual fidelity still need real-device browser inspection in Light and Dark.


## Marketing frame chrome during Rose transition (23 September 2026)

Follow-up to the single-veil glow fix: the owner still saw a large rectangular 'body' during door-to-page navigation. The individual landing and five framed marketing routes intentionally have a 90vw centered frame with a translucent fill, Rose border, shadow and backdrop blur; those frame edges can show through a semitransparent full-viewport veil even when the veil itself no longer has a rectangular scaled layer. All six route-level frame containers now have `data-dc-marketing-frame` (a styling hook only). The shared CSS at `app/globals.css` temporarily dissolves just that decorative frame chrome whenever `document.documentElement.dataset.dcMarketingTransition` is present, restores it smoothly on reset, and leaves DOM, content, frame geometry/overflow, page-specific navbar/footer and all click/scroll behavior unchanged. Apply this rule only while a marketing transition is active, never as a permanent page restyle. Do not modify `SimpleDoorLab` mesh/portal/zoom/orbit or marketing sound/route durations to hide these rectangular frame edges. Confirm in local desktop/mobile browsers whether any remaining box is a different source before making more changes.


- **Dashboard reference update (23 September 2026):** The later screenshot-driven Rose rail explicitly supersedes earlier neutral-sidebar instructions. Keep the selected-only curved-outside navigation shape scoped to the shared DashboardSidebar, maintain distinct hover/active states, and favor genuine event data and functions over filler text.


- **Customer dashboard redesign in progress (23 September 2026):** `Dashboard-redesign.md` is the implementation checklist and status record. The sidebar is a single `DashboardSidebar` component shared by mobile/desktop; curved-outside hover/active facets and hover colors are owned by scoped `dc-dashboard-sidebar-nav` CSS. Never restore the two overlapping generic sidebar style blocks or style the expanded Acara parent as selected alongside its child. Shared `DashboardPrimitives` define the card/page hierarchy. Prefer actual event/RSVP data and functional actions over repeated eyebrow/title/description copy; never fabricate graphs. Public landing/door remain untouched. Build and visual validation are pending until recorded in the redesign checklist.

- **Screenshot-driven dashboard redesign (23 September 2026):** See `Dashboard-redesign.md`. The Beranda hero gives the Tambah acara action a clear place; compact recent-event rows replace the stretched empty table, while RSVP and publication charts retain real values. Dashboard pages share updated `DashboardPrimitives`; the hero's white CTA is a scoped contrast exception to canonical Button styling. Browser visual QA remains pending.

- **WA Blast template authoring (23 September 2026):** Acara → WA Blast includes a real event-owned draft template CRUD flow, with invitation/RSVP reminder/event reminder/thank-you presets. Allow only {nama}, {acara}, {tanggal}, {lokasi}, {link}. Render preview from actual selected recipient/event fields; sample preview must be identified and must not be copied as though real recipient data. Message copy is **not** a sent WA blast and must not deduct quota. Do not add fake delivery/automation controls before provider-backed sending and credit-ledger APIs exist. Run `pnpm db:deploy` on the target database after this schema migration. Do not clone the third-party screenshot's magenta layout; keep DC Organizer Rose styling.


- **Framed Beranda/account settings (23 September 2026):** Customer `/dashboard` uses one Rose-outline responsive mainframe holding the shared sidebar, navbar and workspace, without copying the public floral ambience or modifying the approved landing/Pintu. The canonical wordmark belongs once in the sidebar and remains unchanged. New menu entries Profil Saya / Pengaturan akun open a functional, event-independent account panel; retain Transaksi, Paket, FAQ, Keluar, theme and language. Profile name and avatar persist to the existing user model, not event media. Authenticated avatar uploads must decode/resize with Sharp and use user-scoped file paths; changing a password must validate the old password, securely hash the new value, and revoke old sessions. Email cannot be edited in this scoped iteration. Run `pnpm db:deploy` for the avatar migration before using the updated Prisma model. Browser/mobile and CI validation must be reported only after actual checks.

- **Mainframe correction / palette supersedes old Rose-rail baseline (23 September 2026):** The latest owner instruction explicitly replaces the screenshot-era solid-Rose sidebar and pale-Rose dashboard canvas: keep the customer Light dashboard and sidebar white, Dark dashboard and sidebar near-black, with visibly Rose outlines, icons, text, active states and global button appearance matching the landing palette. `dc-dashboard-frame` must be height-bounded like the approved marketing frame; do **not** use `min-height` to let document scroll extend the frame below the viewport. Navbar/brand/sidebar stay in the bounded frame while only `dc-dashboard-scroll` scrolls vertically; mobile drawer/scrim also remain inside. Do not break page content, event state, profile/security behavior, landing/Pintu or other app areas. This replaces older rail Rose/white-text and pink canvas dashboard instructions where inconsistent.

- **Solid Rose outer frame + card accent hierarchy (23 September 2026):** The customer `/dashboard` exterior (outside its fixed marketing-style frame) is solid `var(--primary)` Rose in BOTH Light and Dark, not neutral/gradient. Keep the interior/sidebar white in Light and black in Dark; use a subtly light frame border for separation. Shared dashboard surfaces, compact metric cards and nested detail cards all use an EQUALLY THICK Rose LEFT accent (`--dc-dashboard-card-stripe: 0.3cm`); do not reintroduce thinner/thicker exceptions for Beranda, RSVP or the hero. The owner later specified three SQUARE corners with ONLY the top-right corner rounded for all small dashboard cards; no metric top accent remains. Never turn entire cards pink just to add accents, duplicate accent rails on metrics, change the scroll model, or apply this to the approved public landing/Pintu. This owner choice supersedes previous neutral outer-background guidance.

- **Dashboard WhatsApp help position (23 September 2026):** Keep the floating `DashboardWhatsAppHelp` button and its choice menu inside `dc-dashboard-frame`, anchored at its lower-right with a comfortable 20–24px inset. It must stay visible when the inner dashboard content scrolls, keep both Christine/Admin routing and hidden contact numbers, and constrain the option panel to the frame on small screens. Do not move it back to the viewport exterior or change the public landing WhatsApp controls.

- **Small-card shape and left-only Rose stroke (23 September 2026):** On the shared customer dashboard, `DashboardSurface`, `DashboardMetricCard`, `DashboardCompactStat`, `DashboardNotice`, `DashboardEmptyState` and Beranda's hero have squared top-left/bottom-left/bottom-right corners; ONLY the top-right is rounded (18–28px according to scale). Rose accent borders must appear vertically on the LEFT with ONE shared width of `--dc-dashboard-card-stripe: 0.3cm` for every card; the former 3px/4px distinction is obsolete. Never restore metric top accent or generic round-all-corners on these surfaces. Do NOT modify the larger outer mainframe, buttons/badges/icon circles, private modal controls or public landing. Preserve scrolling, interactions, actual data, Light/Dark palette and WhatsApp placement.

- **All-dashboard equal card stripe (23 September 2026, latest owner rule):** Replace older 3px/4px card-accent guidance with a **single shared 0.3cm left Rose border** for all actual dashboard cards, both shared primitives and explicitly marked `dc-dashboard-detail-card` in event/invitation/personal invitation/WA Blast/RSVP/guest placement/Usher/account. Keep top/right/bottom hairline borders and ONLY top-right rounded. Do not apply thick left stripes to form controls, badges, buttons (except explicit template-selector card), QR modals, the interactive seating stage, navigation or the outer mainframe. Preserve usability of hover/selection on WA template selector and draggable guests. Dashboard Light interior white, Dark interior near-black, exterior solid Rose. Maintain same stripe token; no page-specific widths or CSS overrides. Shared source and browser validation pending.

- **Beranda gradient without circles (23 September 2026):** Keep the Beranda welcome/hero card's Rose-tinted linear gradient but do NOT add circular outline rings, decorative `::after` geometry, or a circular/radial gradient glow. This applies to both Light and Dark. Preserve the shared 0.3cm left card stripe, single top-right radius, actual content and CTA; do not change the functional RSVP circular progress chart.


- **No card-in-card dashboard composition (23 September 2026, latest owner correction):** On `/dashboard`, a content section must NOT draw an enclosing bordered/Rose-accented surface around individual metric/event/invitation/recipient/RSVP/guest cards. `DashboardPanel` intentionally renders a FLAT semantic `section` with its title/actions on the workspace canvas and no card fill, border, radius, side stripe or padding around its children. Render standalone individual cards directly under it. Event lists also use a flat section, and a standalone empty state is not wrapped in `DashboardSurface`. Keep single-purpose standalone cards for account forms, Beranda summary panels with unboxed rows, and the functional seating stage. Inline editing fields within a card should be a divider-separated row, not another bordered card. The single `0.3cm` LEFT Rose stripe / top-right-only radius still applies to ACTUAL cards, not their section containers; outer customer mainframe remains unchanged. Preserve APIs, metrics, tab routing, WhatsApp and approved public landing/Pintu.


- **LATEST dashboard hierarchy — large frames ONLY, plain internal rows (23 September 2026):** Owner explicitly corrected the previous flat-section iteration because it produced too many small framed cards. This rule OVERRIDES both the old card-in-card design and the more recent instruction to render `DashboardPanel` as an unboxed section. `DashboardPanel` must draw **one LARGE** Rose-accented section frame around its title, controls and content. All event/invitation/RSVP/WA Blast/personal-invitation/guest/Usher items INSIDE that panel are **plain rows with thin separators**, not `0.3cm` striped/rounded mini-cards. `DashboardMetricGrid` must itself be ONE large framed metrics group with numbers in plain unframed columns (no four independent metric cards). The `0.3cm` Rose left stripe and top-right-only radius belong to each top-level large panel, not its nested records. Beranda's hero, metrics group, recent-event panel and RSVP/publication panel remain peer-level LARGE cards; account form sections are standalone large panels. Seating stage remains usable without a second visual border. Maintain clear hover/selected treatment for interactive WA template rows and draggable guests. Do not modify outer mainframe, landing/Pintu, existing features/data, Light/Dark theme, WhatsApp help placement or inner scrolling. Inspect `DashboardPrimitives.tsx` and the scoped `app/globals.css` single-frame overrides before changing layout again.

- **Dashboard large-frame headings & action buttons (23 September 2026):** Titles of ALL top-level Dashboard frames (`DashboardPanel`, Beranda large summary frames, Owner/Account large panels) use Rose `var(--primary)` and display-only Title Case; keep official acronyms (`RSVP`, `WA`, `DC`, `VIP`) and do not capitalize entire descriptions, guest-message bodies or DB data. Use `DashboardSectionHeader` and scoped frame-header rules in `app/globals.css`, NEVER blanket-color all row headings. ALL ordinary workspace CTA/action buttons and button-like route links must use canonical `Button` (`asChild` for `Link`) with solid Rose background, white text in Light Mode, black in Dark Mode, and shared hover/focus. Do not reintroduce white/neutral `bg-background` + Rose-text button look or global grid CSS that overrides Button; simple WA Blast placeholder insertion actions must also use Button. Preserve the existing distinct navigation/account menu chrome, icon-only and selectable-list row geometry, input/filter controls, disabled states, frame hierarchy, landing/Pintu and invitation artwork.

- **Guest Management navigation (23 September 2026):** The customer dashboard sidebar must display **Manajemen Tamu** as an expandable/collapsible group containing exactly two submenu entries in this order: **Undangan Personal** (`personalInvitation`, reuses the existing PersonalInvitationPanel and all its data/actions) and **Pengaturan Meja** (`placement`, reuses the existing PlacementWorkspace, guest/table fetches, seating and permissions). Remove `personalInvitation` from the **Acara** group and remove the previous standalone `placement` entry. Keep RSVP and Usher App as independent sidebar entries. Keep tab IDs/API/routes unchanged, auto-expand Manajemen Tamu when navigating to either submenu, preserve active/accessibility state and mobile sidebar behavior. ID labels are default, with English `Personal Invitation` / `Table Setup`. Do not redesign the approved large-frame layout while moving navigation.

- **Undangan Personal canonical Guest profile (23 September 2026, NEW):** Every personalized invitation, RSVP, WA Blast selection, seating row and Usher check-in references the **same existing `Guest.id` scoped by `invitationId`**. Never introduce a second personal-invitation guest/recipient table, duplicate canonical name/phone/category/group, or copy RSVP attendance into invitation quotas. Keep `Guest.name` + `phone` canonical; reuse existing `Guest.category` (Reguler/VIP/VVIP/custom legacy) and `Guest.tags` (group labels). New guest-only optional fields: `personalAddressee` (envelope display), `recipientType` (individual/couple/family/group), `invitedPax` (1–30 INVITED people, not actual attendance), `personalGreeting`, `personalSharedAt` (manual share marker; NEVER claim WhatsApp delivered). `Guest.plusOnes` remains RSVP-confirmed companions and `rsvpStatus` and `checkedIn` remain independent statuses. Create form selects an existing guest or adds one; save/edit share validation in `lib/guests/personal-profile.ts`, data in `/api/personal-invitations` and `/api/guests`. Personalized public templates MUST pass Guest.id + personal token and invitedPax to RSVP so confirmation UPDATEs this same Guest (no new guest); require valid guest token, enforce RSVP quota and do not allow dropping quota below confirmed attendance. Parent event must be published before sharing personalized link. Existing guest-level seating/check-in represents ONE guest/recipient row, NOT automatically a seat for every invited companion; future multi-seat planning must use explicit attendee/seat modeling, not duplicate Guests or overwrite plusOnes. Existing large-frame dashboard design and landing/Pintu remain unchanged. Sync local database after pull with `pnpm db:deploy` then `pnpm db:generate` before local build; CI generates Prisma client automatically. The personal link's "dibagikan" is a manual marker only, not proof that WA Blast/WhatsApp sent or delivered.

- **Frame title + action button consistency (23 September 2026):** All Dashboard operational frame/section titles use Rose (`var(--primary)`) and display-only Title Case. The canonical `Button` primitive carries the `dc-app-button` marker; inside Dashboard/Usher `<main>`, canonical actions must render as the filled DC Rose button (Light: white text; Dark: black text) regardless of legacy local outline/white-background classes. Do not apply this enforcement to approved navbar/header/account controls, icon-only close controls, invitation artwork, status badges, or ordinary text links. New operational actions should use `components/ui/button.tsx`, not ad-hoc `<button>`/styled anchors.

- **LATEST canonical guest data flow (23 September 2026):** One `Guest` row per actual recipient/party *per Invitation event*, shared by Undangan Personal, RSVP, Pengaturan Meja, WA Blast, dashboard analytics and Usher. Do not create copy recipient/contact tables or duplicate `Guest` records when adding WA recipients. `Guest.id` is the common key; `recipientType`, `invitedPax` (invitation allowance), `category`, `tags` (groups), `personalAddressee`, `personalGreeting` and `personalSharedAt` are already on Guest and have existing Prisma migration files `20260923124000_guest_personal_invitation_details` + `20260923124500_backfill_personal_invitation_quota`. Never conflate `plusOnes` (confirmed RSVP companions) with `invitedPax`. Use `parsePersonalGuestFields` for shared field validation and `findGuestsByContact` to detect likely same-event name/phone duplicates including 08/+62 formats. Owner-authenticated WA Blast may reuse a unique existing Guest; multiple matches require manual disambiguation. **Security:** a public generic RSVP link MUST NOT overwrite an existing Guest using only name + phone; it must reject a matching contact and require a valid personal recipient token or organizer assistance. Public RSVP responses must not expose guest passwords/hashes or personal tokens. Preserve audit/checkin and event-scoped authorization; legacy guest-management/Usher routes must resolve event from Guest.id, NEVER automatically edit the account's first event.
- **Shared active event (23 September 2026):** The customer dashboard maintains one active event across Undangan Personal, WA Blast, RSVP, Pengaturan Meja, and Usher via `selectEvent`. Re-entering RSVP/placement/Usher refreshes guests from `/api/guests?invitationId=...`; returning to Beranda refreshes context. The standalone Usher page and its `/api/usher/guests`/`checkin` endpoints use the selected event ID and verify QR ownership/event match before writing the same canonical Guest check-in (atomically with audit fields). No role/entitlement or pre-publish RSVP/guest access changes. Never claim the deployment database has had a migration applied just because migration SQL exists or CI generated Prisma types.


- **Studio canvas and 15 visibility controls (23 September 2026):** Follow PRD §7.2.1a. Studio has one bounded Rose-framed shell, independent inspector/canvas scrolling, quiet tool navigation, collapsible desktop inspector, and mobile Pengaturan/Undangan views. Keep application font/brand/button tokens; do not copy landing decorations or add stacked duplicate Studio/event headings. `lib/templates/sections.ts` owns all 15 visibility controls including envelope and music; preserve old `sections=` values and default new flags ON, store additional hidden flags in `hidden=` without schema changes. Both real renderers must honor them. Template capability locks (Romantic Rose palette/font) must be honest. Display font names in actual loaded font families, never Aa Bb. Keep watermark in portalled previews, focus/Escape handling, server auth/payment gates, and event-scoped save. Prevent publishing unsaved design and never submit stale isPublished during design-only saves.

- **Studio defaults and music uploads (23 September 2026):** Restore via “Kembalikan ke Default” resets current theme palette/font/15 visibility flags only, preserving content/media and allowing Undo before Save. Music upload limits are 2 AUDIO assets per Invitation, 3 MiB each, validated client/server using `lib/invitations/audio-limits.ts`; serialize quota count+create under an Invitation row lock. Deleting audio frees its slot and clears that selected musicUrl, with safe generated-file cleanup. Built-in songs do not consume slots; existing over-limit assets are retained until the user deletes them. Reject URL-only new asset creation; retain legacy musicUrl compatibility. Photo Sharp WebP pipeline remains unchanged. Do not claim Wishes persistence or full browser coverage.

- **Invitation typography/palette and visible names (24 September 2026):** Actual envelope/cover Names must consume the selected invitation font, including next/font CSS tokens for Cinzel/Fauna One. Custom palette variables apply to envelope/cover base surfaces, ink and accents; keep original artwork fallbacks for the preset. Use readable text against light/dark custom surfaces. Render host/couple names, event titles and personal recipient gate names with shared `displayTitleCase`, never mutate stored names or capitalize descriptions/guest messages. Remove forced uppercase on name-only scene elements. Romantic Rose retains its declared palette/font lock. Wishes must disclose that submission remains unavailable. Design saving shares the music asset row lock and rejects deleted local audio URLs; loading failures must show retry rather than endless preview loading.


## RSVP confirmation / QR convention (24 September 2026)

RSVP success must follow committed Guest state for all three statuses, independently of QR availability. On-page confirmation is free and automatic; do not add WA sends/credits. Guest download QR must use the same signed Usher token and same-origin PNG endpoint with event/attendance/entitlement validation, never a third-party QR renderer. Keep actual guest tokens out of docs, fixtures and public preview links. Visible RSVP dashboard polling must clean up on event/tab change and preserve last successful data on temporary failure.
